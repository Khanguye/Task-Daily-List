# Homepage Dynamic Index Prompt

Fix `index.html`.

Requirements:

- Make `index.html` the master HTML page.
- List all HTML pages in the website except `index.html`.
- Make the links dynamic so if a second or third HTML page is added and pushed, the homepage picks it up automatically.
- Use the GitHub repository contents API for the dynamic page list.

