# RadixSort Prompt

Create an HTML page named `RadixSort`.

Requirements:

- Demonstrate radix sort with editable input.
- Show buckets for digits `0` through `9`.
- Animate each digit pass from least significant digit to most significant digit.
- Include `Run Animation`, `Step`, and `Reset` controls.
- Explain the approach.
- Explain why radix sort works.
- Include pseudocode.
- Compare running time and space complexity.
- Output: animation UI.

